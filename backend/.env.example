# Server Configuration
PORT=5000
NODE_ENV=development
FRONTEND_URL=http://localhost:5173

# Authentication Secret
JWT_SECRET=super_secret_erp_crm_jwt_key_2026_change_in_production
JWT_EXPIRES_IN=24h

# Database Configuration (XAMPP Local Default or Cloud MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=mini_erp_crm

# Alternative Database Connection URL (Optional - e.g. for Aiven / Railway MySQL)
# DATABASE_URL=mysql://user:password@host:port/mini_erp_crm?ssl-mode=REQUIRED
